1) Create a new project and then click on the project and configure build path and then import the libraries jai-codec.jar , jai core 1.1.3 .jar

2) Places all the images into the folder.

3) Copy both the imagecomparision.java and JPEGImageFile.java into the project.

4.) Change the path to the image destination folder where ever required.

4) run the imagecomparision.java and then select the first image and then select the second image 

5) click on any part of the image and thereby we would get the highlighted part of the second image.